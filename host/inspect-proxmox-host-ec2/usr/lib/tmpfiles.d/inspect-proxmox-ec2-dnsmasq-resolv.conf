# PVE launches per-zone dnsmasq with -r /run/dnsmasq/resolv.conf for upstream DNS. On EC2
# that file doesn't exist, so point it at the VPC resolver (second IP in the VPC CIDR).
d /run/dnsmasq 0755 root root -
f /run/dnsmasq/resolv.conf 0644 root root - nameserver 169.254.169.253
